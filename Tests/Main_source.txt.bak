procedure main {
	x = 5;
    age = 21 + 20 - 4 * 3 / (1+4);
    read max;
    while (age < max) {
        if (age/2 < 20) {
            print x;
        } else {
            print age;
        }
        age = age + 1;
        print age;
    }
}
